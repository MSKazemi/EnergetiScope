# Deployment | CPU-bound | 1000m (1 core) | 512Mi memory
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bench-cpu-1000m
  namespace: energetiscope-bench
  labels:
    app: energetiscope-bench
    bench-profile: cpu
    bench-cpu: "1000m"
    bench-mem: "512Mi"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: energetiscope-bench
      bench-workload: bench-cpu-1000m
  template:
    metadata:
      labels:
        app: energetiscope-bench
        bench-workload: bench-cpu-1000m
    spec:
      containers:
        - name: stressor
          image: polinux/stress-ng
          command: ["stress-ng", "--cpu", "2", "--timeout", "600s"]
          resources:
            requests:
              cpu: "1000m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
      terminationGracePeriodSeconds: 5
